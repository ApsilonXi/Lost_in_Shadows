import pygame

# Окно и другие настройки
SCREEN = pygame.display.set_mode((0, 0), pygame.FULLSCREEN)
WIDTH, HEIGHT = SCREEN.get_size()

# Цвета
WHITE = (255, 255, 255)
GOLD = (255, 248, 166)
RED = (255, 0, 0)
GREEN = (0, 255, 0)
BLACK = (0, 0, 0)
